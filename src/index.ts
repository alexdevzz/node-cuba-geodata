import { getCities, addCity } from './services/cityService'
import { getProvinces, addProvince } from './services/provinceService'
import { getTowns, addTown } from './services/townService'

export {
  getCities, addCity,
  getProvinces, addProvince,
  getTowns, addTown
}

export default {
  getCities,
  getProvinces,
  getTowns
}

// test comment to CI_CD 2
